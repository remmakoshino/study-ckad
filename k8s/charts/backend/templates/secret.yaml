# CKAD学習ポイント: Secret - 機密情報の管理
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "backend.fullname" . }}-secret
  labels:
    {{- include "backend.labels" . | nindent 4 }}
type: Opaque
data:
  mongodb-uri: {{ .Values.env.MONGODB_URI | b64enc | quote }}
