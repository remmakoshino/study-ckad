# CKAD学習ポイント: ConfigMap - 環境変数の外部化
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-config
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.env.PORT | quote }}
  MONGODB_DATABASE: {{ .Values.env.MONGODB_DATABASE | quote }}
