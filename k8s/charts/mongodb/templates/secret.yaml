apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mongodb.fullname" . }}-secret
  labels:
    {{- include "mongodb.labels" . | nindent 4 }}
type: Opaque
data:
  root-username: {{ .Values.auth.rootUsername | b64enc | quote }}
  root-password: {{ .Values.auth.rootPassword | b64enc | quote }}
