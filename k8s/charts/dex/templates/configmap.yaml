# CKAD学習ポイント: ConfigMap - アプリケーション設定の外部化
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dex.fullname" . }}-config
  labels:
    {{- include "dex.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
